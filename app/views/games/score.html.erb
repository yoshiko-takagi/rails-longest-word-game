<div class="container bt-3">
  <h1>Result</h1>

  <% if @included %>
    <% if @english_word %>
    <p><strong>Congrats!</strong> <%= @word %> is a valid English word!</p>
    <% else %>
      <p>Sorry but <strong><%= @word %> does not seem to be a valid English word.</strong></p>
    <% end %>
  <% else %>
    <p>Sorry but <strong><%= @word %></strong> can't be built out of <%= @letters.join(", ") %></p>
  <% end %>

  <%= link_to "Play again", new_path %>
</div>
